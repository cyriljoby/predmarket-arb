# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Secrets / credentials — NEVER commit
.env
*.pem
*.key
*private*key*

# Project data outputs (generated at runtime)
# Collected data and derived reports — regenerated, never committed. Globs
# rather than exact names so archived/era-split variants cannot leak in.
opportunities*.jsonl
matches*.json
reviews.json
backtest_report*.json

# Project spec / private instructions
CLAUDE.md

# OS / editor
.DS_Store
.idea/
.vscode/

# Collection run logs and one-off backups
*.log
*.bak
matches_prededupe.json

# Local DB volume is managed by docker compose, never committed
